[package]
name = "novaos-kernel"
version.workspace = true
edition.workspace = true

[dependencies]
bootloader_api.workspace = true
linked_list_allocator = "0.10.5"
novaos-arch = { path = "../arch" }
novaos-hal = { path = "../hal" }
