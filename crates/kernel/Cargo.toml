[package]
name = "nastalli-kernel"
version.workspace = true
edition.workspace = true

[dependencies]
bootloader_api.workspace = true
linked_list_allocator = "0.10.5"
nastalli-arch = { path = "../arch" }
nastalli-hal = { path = "../hal" }
